<br>
<table>
  <tr>
    <th>Name</th>
    <th>Fee</th>
    <th colspan="2"></th>
  </tr>
 
  <% @plans.each do |plan| %>
    <tr>
      <td><%= plan.name %></td>
      <td><%= plan.fee %></td>
      <td><%= link_to 'Subsctibe Plans', user_subscriptions_path(:user_id => user.id, :plan.id => plan.id)%></td>
    </tr>
  <% end %>
</table>
